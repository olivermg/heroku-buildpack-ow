#!/usr/bin/env bash

BUILD_DIR=$1

cat << EOF
---
config_vars:
  PATH: /usr/local/bin:/usr/bin:/bin
  LANG: en_US.UTF-8
default_process_types:
  web: echo $(ls -lad ${BUILD_DIR}/bin/run.sh); /bin/bash ${BUILD_DIR}/bin/run.sh
EOF

