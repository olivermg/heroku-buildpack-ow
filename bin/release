#!/usr/bin/env bash

BUILD_DIR=$1

cat << EOF
---
addons:
  - shared-database:5mb
config_vars:
  PATH: /usr/local/bin:/usr/bin:/bin
  LANG: en_US.UTF-8
default_process_types:
  web: ${BUILD_DIR}/bin/run.sh
EOF

