#!/usr/bin/env bash

# usage: bin/compile <BUILD_DIR> <CACHE_DIR> <ENV_DIR>
# see also https://devcenter.heroku.com/articles/buildpack-api

echo "BUILD_DIR: ${BUILD_DIR}"
echo "CACHE_DIR: ${CACHE_DIR}"
echo "ENV_DIR: ${ENV_DIR}"

